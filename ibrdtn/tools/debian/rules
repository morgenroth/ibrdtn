#!/usr/bin/make -f

include /usr/share/cdbs/1/rules/debhelper.mk
include /usr/share/cdbs/1/class/autotools.mk

# extra configure flags
DEB_CONFIGURE_EXTRA_FLAGS := --with-bash-completion-dir=/etc/bash_completion.d

# debug flags
DEB_DH_STRIP_ARGS := --dbg-package=ibrdtn-tools-dbg

