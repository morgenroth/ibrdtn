# Defaults for ibrdtnd initscript
# sourced by /etc/init.d/ibrdtnd
# installed at /etc/default/ibrdtnd by the maintainer scripts

# Set to '0' to explicitly disable starting IBR-DTN daemon
#ENABLED=0

# Additional options that are passed to the Daemon.
DAEMON_OPTS="-c /etc/ibrdtn/ibrdtnd.conf"

# Directory for additional configuration, e.g. security keys and certificates
DAEMON_CONFDIR="/etc/ibrdtn"

# Run the daemon as this specific user
DAEMON_USER="dtnd"
